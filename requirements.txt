# Full installation dependencies, including optional PyMuPDF-based PDF backends.
# If PyMuPDF / pymupdf4llm fails on Windows + Python 3.12, install
# requirements-core.txt instead and use --pdf-mode pypdf_text.

pymupdf4llm>=0.0.17
pymupdf>=1.24
pypdf>=4.0
langchain>=0.2
langchain-core>=0.2
langchain-ollama>=0.1
pydantic>=2.0
pandas>=2.0
openpyxl>=3.1
